package mtx

import (
	"testing"
)

func TestMappedAttributeType(t *testing.T) {
	// r := NewTypeRegistry("testclass")
	// td := Datatype{
	// 	Named: N("testclass", "Integer"),
	// }
	// td.AttributeDatatype = &Integer
	// td.Set("bits", 42)
	// r.Add(td)

	// rd := r.MappedAttributeType(Integer)
	// if got, want := rd.GetInt("bits", 0), 42; got != want {
	// 	t.Errorf("got [%v]:%T want [%v]:%T", got, got, want, want)
	// }
}
